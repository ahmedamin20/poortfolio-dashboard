import { Link } from "react-router-dom";

const LoginView = ({
  handleSubmit,
  handleChange,
  formikValues,
  formikErrors,
  isSubmitting = false,
}) => {
  return <div className=""></div>;
};

export default LoginView;
