import {Circle} from "react-feather";
import experienceRoutes from "../constants/routes";

const sideBar = [
    {
        id: "experience",
        title: "Experiences",
        icon: <Circle size={20} />,
        navLink: experienceRoutes.TABLE
    }
];

export default sideBar